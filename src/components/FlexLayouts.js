import React from 'react';
import { View } from 'react-native';

function FullFlex(props) {
    return (
        <View style={{flex: 1}} {...props}/>
    );
}

function HalfFlex(props) {
    return (
        <View style={{flex: 1}} {...props}/>
    );
}

function ThirdFlex(props) {
    
}